\subsection{Analyse der HTTP/1.1-Kommunikation}

Die HTTP/1.1 Kommunikation wurde analog zu der HTTP/2 Kommunikation aufgenommen~\ref{enum:wireshark_capture_method}.

Der Aufbau der HTTP/1.1 Pakete ist wie nach dem Standard zu erwarten~\ref{code:http11-message-format}.

\codeblock{res/http11_message_format.txt}{HTTP/1.1 Message Format~\cite{rfc9112_http11}}{code:http11-message-format}

Dabei besteht die HTTP/1.1 Nachricht aus einem Start Line, gefolgt von Headern und einem optionalen Body.
Die Start Line besteht aus einem Method, einem Request Target und einer HTTP Version.
Die Header bestehen aus einem Header Name und einem Header value getrennt durch einen Doppelpunkt, die Tabelle~\ref{tab:http11-header-request} zeigt den Aufbau der Get Anfrage in der pcapng Datei.

\begin{table}[h]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Start Line} & GET /index.html HTTP/1.1\textbackslash r\textbackslash n \\
        \hline
        \textbf{Header Name} & \textbf{Header Value} \\
        \hline
        Host & cuil.internet-transparency.org \\
        Accept & */* \\
        Accept-Encoding & gzip, deflate \\
        Connection & keep-alive \\
        User-Agent & python-httpx/0.28.1 \\
        \hline
    \end{tabular}
    \caption{HTTP/1.1 Get Anfrage in der pcapng Datei}
    \label{tab:http11-header-request}
\end{table}

Der Payload bei der Anfrage ist 0 Bytes groß, da es sich um eine Get Anfrage handelt, die nur aus dem Header besteht.

\begin{table}[h]
    \centering
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Start Line} & HTTP/1.1 200 OK\textbackslash r\textbackslash n \\
        \hline
        \textbf{Header Name} & \textbf{Header Value} \\
        \hline
        Accept-Ranges & bytes \\
        Content-Length & 1056 \\
        Content-Type & text/html; charset=utf-8 \\
        Etag & "da02fmydfj7ktc" \\
        Last-Modified & Mon, 19 May 2025 10:30:02 GMT \\
        Server & Caddy \\
        Vary & Accept-Encoding \\
        Date & Mon, 25 May 2026 15:16:14 GMT \\
        \hline
        \textbf{Body} & 1056 Bytes \\
        \hline
    \end{tabular}
    \caption{HTTP/1.1 Antwort in der pcapng Datei}
    \label{tab:http11-header-response}
\end{table}

Die HTTP/1.1 Antwort besteht aus einem Start Line, gefolgt von Headern und einem Body. Dabei ist der Payload (Body) 1056 Bytes groß, wie im Content-Length Header angegeben~\ref{tab:http11-header-response}.
Die HTTP/1.1 Kommunikation in der pcapng Datei entspricht somit dem Standard und es gibt keine Auffälligkeiten oder Besonderheiten anzumerken.

Für die ganze Kommunikation wurden 12 Frames auf der Verbindungsschicht ausgetauscht, davon waren 10 TCP Pakete und 2 HTTP Pakete.

\subsubsection{DNS Cache in der pcapng}
Unabhängig von der HTTP/1.1 Kommunikation fällt in der pcapng Datei eine Besonderheit auf.
Die TCP Verbindung zum Server wird über IPv6 gestartet bevor die Antwort des DNS Servers die URL in eine IPv6 Adresse aufgelöst hat.
Das is in sofern ungewähnlich, da der Client diese die Adresse hinter der URL eigentlich erst nach der DNS Abfrage kennen sollte, um die TCP Verbindung zum Server zu starten.
Die wahrscheinlichste Erklärung ist, dass die IP Adresse bereits in einem der DNS Caches lag.
DNS Anfragen werden auf vielen Ebenen gecached, wie zum Beispiel im Betriebssystem, im Browser, Router oder in einem lokalen DNS Server.
Wobei in unserem Fall wahrscheinlich der DNS Cache des Betriebssystems die IP Adresse vorhielt, da kein Browser genutzt wurde und Kommunikation mit dem lokalen DNS Server oder dem Router offensichtlich nicht abgewartet wurde.
